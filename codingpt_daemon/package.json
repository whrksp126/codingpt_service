{
  "name": "@codingpt/runner-monorepo",
  "version": "0.1.0",
  "description": "CodingPT 러너 모노레포 — runner-core(공유) + daemon(로컬) + cloud-runner(클라우드)",
  "private": true,
  "workspaces": [
    "packages/runner-core",
    "packages/daemon",
    "packages/cloud-runner",
    "packages/cpt-cli"
  ],
  "scripts": {
    "daemon": "node packages/daemon/index.js",
    "daemon:run": "node packages/daemon/index.js run",
    "test": "CPT_SHIM_NO_GLOBAL_LINK=1 node --test packages/runner-core/test/*.test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "UNLICENSED"
}
